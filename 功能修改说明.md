# 理论力学学习平台 · 功能修改说明

> 项目路径：`C:\Users\Lenovo\Desktop\MechMind-main\MechMind-main`
> 修改日期：2026-08-15

本次共修改两个文件，新增一个功能、修复一处布局问题。

---

## 一、个人知识网络（`personal_knowledge.html`）

### 新增功能：点击知识节点弹出该知识点的全部学习记录

在「个人知识网络」图中，点击任意知识节点，会弹出详情框，列出与该知识点相关的学习记录：

| 分区 | 内容 |
|------|------|
| 🔗 关联知识点 | 通过关系图/网络边找到的相关知识点 |
| 💬 学习对话 | AI 智能问答中的历史对话（含用户当时的问题） |
| 📒 学习笔记 | 学习整理产生的笔记（第一条可编辑/确认） |
| 💭 用户思考 | explicit / inferred 两类用户想法 |
| ⚠️ 错误理解 | 错误类型与描述 |
| 📊 掌握状态 | 各能力维度的掌握程度 |
| 🎯 薄弱点 | 薄弱点判断与反馈（可判断准确/不准确） |
| 📌 学习事件 | 学习行为事件 |

### 关联知识点的三个来源

1. **AI 问答跨领域关联图**（`RELATED_TOPICS`，59 个知识点，从 `ai_qa.html` 提取）
2. **个人知识网络中的连线**（`graph.edges`，含内置示例的边）
3. **学习记忆里的概念关系**（后端 `/api/learning-memory` 的 `concept_relations`，最多扩散 2 层）

### 学习记录的数据来源

- **AI 问答对话**：浏览器本地 `localStorage` 的 `qa_conversations_<账户ID>`
- **结构化学习记录**（笔记/思考/错误/薄弱点）：后端 `/api/learning-memory/<用户ID>`（`pdata/memory_*.json`）
- **复习记录**：`localStorage` 的 `learning_log_<账户ID>`

### 使用前提

1. 两个页面需在**同一域名**下打开（用服务器访问，不要直接双击 `file://` 打开），否则不共享本地存储。
2. 需**同一账号**（数据按账户隔离）。
3. 提交问题后回到本页需**重新点击节点**才会读到最新记录（无跨页面实时推送）。
4. 「笔记/思考/错误/薄弱点」等结构化记录需要启动后端服务器，并在 AI 问答里点「整理本次学习」生成。

### 关键函数

- `collectLearningRecords(node, state)`：汇总并渲染某知识点的全部学习记录
- `mergeQaConversations(state)`：读取本地 AI 问答对话并合并到学习记录
- `buildLocalState()`：服务器不可用时的本地兜底数据
- `RELATED_TOPICS`：AI 问答跨领域关联图（从 `ai_qa.html` 提取注入）

---

## 二、AI 智能问答（`ai_qa.html`）

### 修复：白色主区域过大 / 出现大块空白

**根因**：`.chat-window` 的 HTML 结构里多了一个 `</div>`，导致「对话区、快捷提问、常见问题、输入框」被排到白色盒子外面，白盒子变成几乎全空的大白块。

**改动**：

1. 删除多余的一个 `</div>`，使 5 个子元素（标题栏、对话区、快捷提问、常见问题、输入框）正确回到 `.chat-window` 内部。
2. 竖直高度减小：`.chat-window` 高度 `84vh` → `76vh`，最小高度 `560px` → `460px`。

**结果**：白色区域从上到下正常排列，不再有大块空白，整体更紧凑。

---

## 三、运行说明

```bash
cd C:\Users\Lenovo\Desktop\MechMind-main\MechMind-main
python server.py
```

- 访问地址：`http://localhost:8080`
- 后端端口：`8080`
- 完整功能（AI 问答结构化学习记录）依赖此服务器；只做问答对话和本机复习记录时，也需同域名访问以便共享 `localStorage`。
