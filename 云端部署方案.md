# 理论力学教学平台 · 云端部署方案

> 面向对象：任课教师 / 课程展示
> 项目：长沙理工大学理论力学研究平台（MechMind）
> 整理日期：2026-08-26

---

## 一、项目概述

本平台是一套基于 **Python + edge-tts** 的理论力学在线教学系统，当前已具备：

| 功能 | 说明 |
|---|---|
| **题库系统** | 596 道理论力学习题（静力学 174 / 运动学 162 / 动力学 206 / 分析力学 54），支持按模块、难度、知识点、题型筛选与全文搜索 |
| **题目解析** | 每道题含题目、答案、详细解析，公式用 KaTeX 渲染 |
| **AI 语音讲解** | 内置 6 种音色（少女音·宵宫、御姐音·克洛林德等），通过 edge-tts 将解析自动合成为中文语音讲解，含解题思路与逐步推导 |
| **平台主站** | 首页、论坛、AI 智能问答、个人知识网络、账户系统等 |

当前系统以**本地服务器**方式运行（`localhost`），本次目标是将其部署到公网，使更多学生通过网址即可访问使用。

---

## 二、当前技术架构

| 组件 | 说明 |
|---|---|
| 运行环境 | Python 3.10 |
| 语音合成引擎 | edge-tts（微软在线神经语音服务，需联网） |
| 题库数据 | 本地 JSON 文件（`qdata/`），含 596 题 + 8 段预生成讲解音频 |
| 运行方式 | 本地服务器，双击启动脚本运行 |

> 说明：AI 讲解的合成原理是「服务器读取题目解析 → 规则引擎把公式总结成人话 → edge-tts 调用微软语音服务合成 mp3 → 返回播放」。其中仅有语音合成一步需要联网，其余（看题、看解析、播放已有音频）完全离线可用。

---

## 三、部署方案选择

### 方案对比

| 方案 | 成本 | 是否需要自备服务器 | 别人使用门槛 | AI 语音讲解 | 适用场景 |
|---|---|---|---|---|---|
| **方案一：本地部署**（现状） | 0 | 否 | 需装 Python + 启动脚本 | ✅ 完整 | 课堂演示、局域网 |
| **方案二：免费 PaaS（Render）** ✅ 推荐 | 0 | **否** | 打开网址即可 | ✅ 完整 | 课程长期使用 |
| **方案三：租用云服务器（VPS）** | ¥30–100/月 | 是 | 打开网址即可 | ✅ 完整 | 大规模 / 需持久化上传 |

### 推荐结论

**采用方案二（Render 免费 PaaS）**：
- 无需租用或自备云服务器，平台自带运行环境；
- 部署完成后得到一个**公网网址**，学生打开网址即可使用，零安装；
- 完全支持 Python 与 edge-tts，云端语音合成正常可用；
- 免费、适合课程教学规模。

> 若未来学生规模增大、或需要「在网站上直接上传内容且长期保存」，可再升级到方案三（付费 VPS，约 ¥30–100/月）。

---

## 四、部署前准备工作

> 以下准备工作已由项目维护方完成，部署时无需重复操作。

1. **单服务器整合**：将题库系统与语音讲解功能整合进主服务器（已整合 TTS 接口：`/api/tts` 试听、`/api/narrate` 生成并存储讲解），云端只部署一个服务。
2. **清理大文件**：移除仓库内超过 GitHub 100MB 限制的压缩包（459MB / 229MB 两个 zip），避免上传被拒。
3. **依赖清单**：编写 `requirements.txt`，声明 `edge-tts` 等依赖，供云端自动安装。
4. **端口适配**：服务器改为读取平台分配的环境变量端口并监听 `0.0.0.0`，适配云端运行环境。

---

## 五、详细部署步骤

### 第 1 步：注册 GitHub 账号（5 分钟）
访问 `https://github.com` 注册账号。若已有账号可跳过。

### 第 2 步：注册 Render（5 分钟）
1. 访问 `https://render.com`；
2. 点击 **Get Started** → 选择 **Sign up with GitHub**，用 GitHub 账号登录。

### 第 3 步：项目上传到 GitHub（10 分钟）
1. 在 GitHub 新建一个仓库（Repository），设为 **Private**（私有）或 **Public** 均可；
2. 将整理好的项目文件夹内容上传到该仓库（上传内容已排除压缩包、`.git`、缓存等无关文件）。

### 第 4 步：在 Render 创建 Web Service（10 分钟）
1. 进入 Render 控制台 → 点击 **New** → **Web Service**；
2. 选择 **Connect** 你的 GitHub 仓库；
3. 按下方配置填写：

| 配置项 | 填写内容 |
|---|---|
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `python server.py` |
| **Instance Type** | Free（免费版） |
| **Port** | 由平台环境变量 `PORT` 自动提供（服务器已适配） |

4. 点击 **Deploy**，等待 2–5 分钟构建与启动。

### 第 5 步：获取公网网址
部署完成后，Render 会分配一个网址，形如：

```
https://xxxxxxxx.onrender.com
```

将该网址分享给学生，即可通过浏览器直接访问平台。

---

## 六、部署后使用说明

| 用户 | 使用方式 |
|---|---|
| 学生 | 打开网址 → 进入题库 → 按模块/难度/知识点筛选 → 查看题目与解析 → 点「生成讲解」在线合成语音 |
| 管理员 | 同网址登录后可上传新题、管理内容 |

---

## 七、内容更新与维护

平台支持持续补充内容，推荐工作流为：

```
在本地添加/修改内容 → 提交推送到 GitHub → Render 自动重新部署
```

- 网址保持不变，学生无需任何操作即可看到更新；
- 已收录的 596 道题、解析及 8 段预生成讲解音频位于代码仓库中，**永久保留**，不会因云端重启丢失。

---

## 八、已知限制与后续升级

| 限制 | 说明 | 升级方案 |
|---|---|---|
| 免费版闲置休眠 | 15 分钟无人访问会暂停，下次访问需等待数秒唤醒 | 可接受，或升级付费套餐 |
| 新生成音频的持久化 | 云端「现场生成的讲解音频」存于临时磁盘，重新部署后会丢失（当时可正常播放） | 升级 Render 付费磁盘 / 换 VPS |
| 上传内容的持久化 | 免费版在网站上直接上传的新题重启会丢 | 付费磁盘 / VPS |

> 建议：课程内容以「本地添加 + 重新部署」方式维护，可完全规避上述持久化问题。

---

## 九、常见问题（FAQ）

**Q1：学生需要安装任何软件吗？**
不需要。学生只需浏览器打开网址即可，无需安装 Python 或任何环境。

**Q2：AI 语音讲解需要什么条件？**
需要联网（edge-tts 调用微软在线语音服务）。若断网，看题、看解析、播放已有音频不受影响。

**Q3：网址会变吗？**
默认不变。除非在 Render 上手动重建服务。

**Q4：免费版能承受多少人同时访问？**
免费实例内存约 512MB，题库为静态 JSON + 轻量请求，几十人规模课程使用没有问题。

**Q5：以后想开放上传功能并长期保存，怎么办？**
将部署升级到付费 VPS（¥30–100/月）或 Render 付费磁盘（约 $7/月），即可实现上传内容持久化。

---

*本文档由项目组整理，供课程部署与评审参考。*
